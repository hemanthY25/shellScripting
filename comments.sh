#!/bin/bash

# any line that starts with hash(#) symbol is reffered as comment in bash.
#bash is a interpreter so, if a line starts with # system it won't executes them# it simply ignore them

echo "comments in bash"
